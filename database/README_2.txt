DB passes order into database as a set of key-value pairs using key: itemID, value: quantity ordered
itemID is defined in our testitemsList table (will be itemsList in production)

Format example:
{itemID: num_of_items, itemID: num_of_items, etc..}